# `examples/` — Example outputs

Small, self-contained example artifacts (a single-subject prediction, a sample
neuro-symbolic rule activation, one ROC curve) so users can see expected output
shapes without running the full pipeline. Generate a quick example with:
`python src/model/main.py --subject S24 --epochs 5 --n-ensemble 1`.
