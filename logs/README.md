# `logs/` — Training & ablation logs

Raw stdout logs from training, evaluation and each ablation variant
(`ablation_abl_*.log`, `losocv_run.log`, `ig_full.log`, …), kept for provenance.
Tracked with Git LFS. New runs append here via the reproducibility scripts.
