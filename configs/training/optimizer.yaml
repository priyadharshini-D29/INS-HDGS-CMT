# -----------------------------------------------------------------------------
# Optimizer configuration.
# Mirrors src/model/config/settings.py (LR / WEIGHT_DECAY).
# -----------------------------------------------------------------------------
optimizer:
  name: adam
  lr: 0.0005                 # 5e-4
  weight_decay: 0.0
  betas: [0.9, 0.999]
  eps: 1.0e-8
  amsgrad: false
