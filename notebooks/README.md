# `notebooks/` — Exploratory notebooks

Jupyter notebooks for exploration and figure prototyping. Notebooks are for
inspection only — the authoritative, reproducible pipeline is the scripts under
`reproducibility/` and `scripts/`. Clear outputs before committing
(`.ipynb_checkpoints/` is git-ignored).
